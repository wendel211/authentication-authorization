# Database Configuration
# PostgreSQL connection settings (matches docker-compose.yml)
DB_HOST=localhost
DB_PORT=5433
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=nest

# JWT Configuration
# IMPORTANT: Change these secrets in production!
# Use strong, random strings (minimum 32 characters recommended)
# You can generate secure secrets using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_ACCESS_SECRET=change_this_to_a_very_strong_random_secret_for_access_tokens
JWT_REFRESH_SECRET=change_this_to_a_very_strong_random_secret_for_refresh_tokens

# Application Configuration (Optional)
# PORT=3000
# NODE_ENV=development
